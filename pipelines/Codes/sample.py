from data_manager.OSM import*
from pipelines.exp_config import *

def sample1(words):
	words = i.get_attribute(words_list).split()
	
def sample2(itemiterator, sample1):
	for i in itemiterator:

		print sample1.w
	
